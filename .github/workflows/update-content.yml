
name: Update Content

on:
  schedule:
    - cron: '0 3,12,15 * * *' # Run daily at 03:00 UTC (12:00 JST), 12:00 UTC (21:00 JST), and 15:00 UTC (00:00 JST)
  workflow_dispatch: # Allow manual trigger

jobs:
  update-content:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch Articles
        run: npx tsx scripts/fetch-articles.ts

      - name: Post to X
        run: npx tsx scripts/post-to-x.ts
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}

      - name: Commit and Push if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add public/search-index.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update search index [skip ci]" && git push)
